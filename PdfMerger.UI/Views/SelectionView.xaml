<UserControl x:Class="PdfMerger.UI.Views.SelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PdfMerger.UI.Views" 
             xmlns:viewmodels="clr-namespace:PdfMerger.UI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SelectionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Button Command="{Binding NavigateToEditPdfViewCommand}" Grid.Column="0" Margin="10,10,10,10">Edit PDF</Button>
        <Button Command="{Binding NavigateToMergePdfViewCommand}" Grid.Column="1" Margin="10,10,10,10">Merge PDF</Button>
        <Button Command="{Binding NavigateToSplitPdfViewCommand}" Grid.Column="2" Margin="10,10,10,10">Split PDF</Button>

    </Grid>
</UserControl>
